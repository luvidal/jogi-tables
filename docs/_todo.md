# Todo

Keep this list short and current. Remove items once shipped.

- [x] **Soft-delete + recycle bin for static tables** — Added to Debts/Assets/BienesRaices/Vehiculos/Inversiones/DeudasConsumo via shared `useSoftDelete` hook, `DeleteDialog`, and `RecycleBin` components in `common/`
- [x] **Keyboard navigation between cells** — Extracted `useGridKeyboard` to `common/`, integrated into Debts, BienesRaices, Vehiculos, Inversiones, DeudasConsumo
